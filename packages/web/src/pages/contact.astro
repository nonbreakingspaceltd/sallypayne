---
import LayoutBase from '../layouts/Base.astro';
import LayoutPage from '../layouts/Page.astro';
import ContactForm from '../components/ContactForm/ContactForm.vue';
import { getPage } from '../data/pages';

const response = await getPage('contact');

const { body, title, image, meta } = response.props;
---
<LayoutBase variant="contact" title={title} meta={meta}>
  <LayoutPage title={title} body={body} image={image}>
    <ContactForm client:idle />
  </LayoutPage>
</LayoutBase>

